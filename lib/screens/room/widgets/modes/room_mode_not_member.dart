import 'package:flutter/material.dart';

class RoomModeViewNonMember extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Not a member')),
    );
  }
}
